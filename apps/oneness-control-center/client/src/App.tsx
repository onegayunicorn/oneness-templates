/* Mineral Signal design: graphite control plane, mineral-green verified signals, asymmetric workspace, visible lifecycle boundaries. */
import { useMemo, useState, type ReactNode } from "react";
import { Link, Route, Switch, useLocation } from "wouter";
import {
  Activity, AlertTriangle, Boxes, BrainCircuit, ChevronRight, CircleDot, Command, Cpu,
  Database, FileCode2, FolderKanban, GitBranch, LayoutDashboard, LifeBuoy, Network,
  PackageCheck, PanelLeftClose, PanelLeftOpen, PlugZap, Rocket, Search, Settings2,
  ShieldCheck, Sparkles, Terminal, Workflow, X
} from "lucide-react";
import { Toaster } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import { ThemeProvider } from "./contexts/ThemeContext";
import Home from "./pages/Home";
import NotFound from "./pages/NotFound";
import { createMockSnapshot, MOCK_SNAPSHOT, requestMockSnapshot, MOCK_BUSINESSES, MOCK_INTEGRATIONS, MOCK_PIPELINES, MOCK_EXECUTIONS, type MockRequestResult, type MockScenario, type Business } from "./lib/mockData";

const sections = [
  { label: "Control plane", items: [
    { href: "/", label: "Dashboard", icon: LayoutDashboard },
    { href: "/applications", label: "Applications", icon: Boxes },
    { href: "/pathways", label: "Pathways", icon: Network },
    { href: "/capabilities", label: "Capabilities", icon: CircleDot },
    { href: "/compose", label: "Compose", icon: GitBranch },
    { href: "/plugins", label: "Plugins", icon: PlugZap },
    { href: "/projects", label: "Projects", icon: FolderKanban },
  ]},
  { label: "Integration plane", items: [
    { href: "/businesses", label: "Businesses", icon: FolderKanban },
    { href: "/integrations", label: "Integrations", icon: PlugZap },
    { href: "/pipelines", label: "Pipelines", icon: Workflow },
    { href: "/executions", label: "Executions", icon: Activity },
  ]},
  { label: "Service plane", items: [
    { href: "/ai", label: "AI Studio", icon: BrainCircuit },
    { href: "/data", label: "Data & Events", icon: Database },
    { href: "/devices", label: "Devices", icon: Cpu },
    { href: "/workflows", label: "Workflows", icon: Workflow },
    { href: "/audit", label: "Audit & Security", icon: ShieldCheck },
  ]},
];

function Brand({ collapsed }: { collapsed: boolean }) {
  return <Link href="/" className="brand" aria-label="ONENESS Control Center">
    <img className="brand-mark-image" src="/manus-storage/oneness-mark_88f7b815.png" alt="" />
    {!collapsed && <span><strong>ONENESS</strong><small>CONTROL CENTER</small></span>}
  </Link>;
}

function Sidebar({ collapsed, onToggle }: { collapsed: boolean; onToggle: () => void }) {
  const [location] = useLocation();
  return <aside className={`sidebar ${collapsed ? "is-collapsed" : ""}`}>
    <div className="sidebar-top"><Brand collapsed={collapsed} /><button className="icon-button rail-toggle" onClick={onToggle} aria-label="Toggle navigation">{collapsed ? <PanelLeftOpen size={16} /> : <PanelLeftClose size={16} />}</button></div>
    <nav className="side-nav">
      {sections.map(section => <div className="nav-group" key={section.label}>
        {!collapsed && <div className="nav-label">{section.label}</div>}
        {section.items.map(item => { const Icon = item.icon; const active = item.href === "/" ? location === "/" : location.startsWith(item.href); return <Link key={item.href} href={item.href} className={`nav-item ${active ? "active" : ""}`} title={collapsed ? item.label : undefined}><Icon size={17} strokeWidth={1.8} /><span>{item.label}</span>{active && <i />}</Link>; })}
      </div>)}
    </nav>
    <div className="sidebar-bottom">
      <Link href="/settings" className="nav-item"><Settings2 size={17} /><span>Settings</span></Link>
      {!collapsed && <div className="rail-status"><span className="live-dot" /> All systems nominal <span className="mono">v0.4.2</span></div>}
    </div>
  </aside>;
}

function Topbar({ onMobileMenu }: { onMobileMenu: () => void }) {
  return <header className="topbar">
    <button className="mobile-menu icon-button" onClick={onMobileMenu} aria-label="Open menu"><Command size={17} /></button>
    <div className="crumb"><span className="mono">workspace</span><ChevronRight size={14} /><strong>control-center</strong></div>
    <div className="top-actions"><span className="mock-live"><span /> MOCK DATA</span><label className="search"><Search size={16} /><input placeholder="Search platform" /><kbd>⌘ K</kbd></label><button className="icon-button notify" aria-label="Notifications"><Activity size={17} /><b /></button><div className="avatar">OG</div></div>
  </header>;
}

function AppShell({ children }: { children: ReactNode }) {
  const [collapsed, setCollapsed] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);
  return <div className="app-shell"><div className={`mobile-scrim ${mobileOpen ? "show" : ""}`} onClick={() => setMobileOpen(false)} /><div className={mobileOpen ? "mobile-open" : ""}><Sidebar collapsed={collapsed} onToggle={() => setCollapsed(!collapsed)} /></div><div className={`main-frame ${collapsed ? "rail-collapsed" : ""}`}><Topbar onMobileMenu={() => setMobileOpen(true)} /><main className="workspace">{children}</main></div><Toaster /></div>;
}

function Composer() {
  const [selected, setSelected] = useState(["identity", "organisations", "authorization"]);
  const modules = ["identity", "organisations", "authorization", "audit", "ai", "risk-scoring"];
  return <div className="page-grid"><div className="page-main"><PageHeading eyebrow="Composition plane / 01" title="Compose the next verified system." description="Resolve a pathway into a dependency-ordered plan before materialization. The graph is the contract." action={<button className="primary-action" onClick={() => setSelected([...selected, "audit"])}><GitBranch size={16} /> Compose application</button>} />
    <section className="panel composer-panel"><div className="panel-head"><div><span className="eyebrow">Pathway</span><h3>Enterprise</h3></div><span className="status-pill green"><span /> foundation</span></div><div className="capability-picker">{modules.map(id => <button key={id} className={`capability-chip ${selected.includes(id) ? "selected" : ""}`} onClick={() => setSelected(selected.includes(id) ? selected.filter(x => x !== id) : [...selected, id])}><span className="chip-check">{selected.includes(id) ? "✓" : ""}</span>{id}<small>{id === "risk-scoring" ? "plugin" : "native"}</small></button>)}</div><div className="graph-stage"><div className="graph-meta"><span>Dependency graph</span><span className="mono">{selected.length} modules / resolved</span></div><div className="graph-line">{selected.map((id, i) => <div className="graph-node-wrap" key={id}><div className={`graph-node ${id === "risk-scoring" ? "plugin-node" : ""}`}><span>{i + 1}</span>{id}</div>{i < selected.length - 1 && <div className="graph-connector" />}</div>)}</div></div></section>
  </div><ContextPanel title="Resolution preview"><Signal label="Unresolved" value="0" tone="green" /><Signal label="Future modules" value="1" tone="amber" /><Signal label="Provenance" value="native + plugin" tone="green" /><div className="context-note"><AlertTriangle size={16} /><p>Materialization stays gated until the plan contains no unresolved capability identifiers.</p></div></ContextPanel></div>;
}

function PageHeading({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) { return <div className="page-heading"><div><span className="eyebrow"><i />{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{action}</div>; }
function ContextPanel({ title, children }: { title: string; children: ReactNode }) { return <aside className="context-panel"><div className="context-title"><span className="eyebrow">Live context</span><h3>{title}</h3></div>{children}</aside>; }
function Signal({ label, value, tone }: { label: string; value: string; tone: string }) { return <div className="signal-row"><span>{label}</span><strong className={tone}>{value}</strong></div>; }
function Stat({ label, value, detail, icon: Icon, accent }: { label: string; value: string; detail: string; icon: any; accent?: string }) { return <div className="stat-card"><div className={`stat-icon ${accent || ""}`}><Icon size={17} /></div><span>{label}</span><strong>{value}</strong><small>{detail}</small></div>; }
function ActivityRow({ title, meta, status, tone = "green" }: { title: string; meta: string; status: string; tone?: string }) { return <div className="activity-row"><div className="activity-mark"><span className={tone} /></div><div><strong>{title}</strong><small className="mono">{meta}</small></div><span className={`status-text ${tone}`}>{status}</span></div>; }
function statusTone(status: string) { const value = status.toLowerCase(); if (value.includes("failed") || value.includes("error")) return "rust"; if (value.includes("degraded") || value.includes("retry") || value.includes("pending") || value.includes("blocked") || value.includes("future")) return "amber"; if (value.includes("draft")) return "muted"; return "green"; }

function ScenarioControl({ scenario, onChange }: { scenario: MockScenario; onChange: (scenario: MockScenario) => void }) { return <label className="scenario-control"><span>Simulate</span><select value={scenario} onChange={event => onChange(event.target.value as MockScenario)}><option value="live">Ready response</option><option value="latency">Slow response</option><option value="empty">Empty response</option><option value="error">Error response</option></select></label>; }
function DashboardStatePanel({ state, scenario, onChange, onRetry, message }: { state: "loading" | "empty" | "error"; scenario: MockScenario; onChange: (scenario: MockScenario) => void; onRetry: () => void; message?: string }) { const loading = state === "loading"; return <div className="state-page"><PageHeading eyebrow={`Control plane / mock ${state}`} title={loading ? "Waiting for the control plane." : state === "empty" ? "Nothing has arrived yet." : "The control plane is unreachable."} description={loading ? "The dashboard is holding its place while the simulated request completes." : state === "empty" ? "The response is healthy, but it contains no pathways, plugins, or recent activity." : message || "The mock adapter returned an error. Retry the request or select another scenario."} action={<ScenarioControl scenario={scenario} onChange={onChange} />} /><section className={`state-panel panel ${state}`} aria-live="polite">{loading ? <><div className="loading-skeleton large" /><div className="loading-skeleton medium" /><div className="loading-grid"><div /><div /><div /></div><span className="state-caption">Simulating {scenario === "latency" ? "1.8s" : "0.65s"} network latency…</span></> : state === "empty" ? <><div className="state-symbol"><CircleDot size={26} /></div><span className="eyebrow">No records</span><h3>Awaiting the first platform event.</h3><p>Use the scenario selector to return to a populated response and continue exploring the control center.</p><button className="secondary-action" onClick={onRetry}><Activity size={15} /> Retry request</button></> : <><div className="state-symbol error-symbol"><AlertTriangle size={26} /></div><span className="eyebrow">Request failed</span><h3>We could not read the mock feed.</h3><p>Retry the request or switch to a different simulated response to test recovery behavior.</p><button className="primary-action" onClick={onRetry}><Activity size={15} /> Retry request</button></>}</section></div>; }
function Dashboard() { const [snapshot, setSnapshot] = useState(MOCK_SNAPSHOT); const [scenario, setScenario] = useState<MockScenario>("live"); const [request, setRequest] = useState<MockRequestResult>({ state: "success", data: MOCK_SNAPSHOT }); const runScenario = (nextScenario: MockScenario) => { setScenario(nextScenario); setRequest({ state: "loading" }); requestMockSnapshot(nextScenario).then(result => { setRequest(result); if (result.data) setSnapshot(result.data); }); }; if (request.state === "loading" || request.state === "empty" || request.state === "error") return <DashboardStatePanel state={request.state} scenario={scenario} onChange={runScenario} onRetry={() => runScenario(scenario)} message={request.message} />; return <div className="page-grid"><div className="page-main"><PageHeading eyebrow="Control plane / overview" title="Operational clarity, from pathway to production." description="A quiet command surface for pathway discovery, capability composition, and governed application generation." action={<div className="heading-actions"><ScenarioControl scenario={scenario} onChange={runScenario} /><button className="secondary-action" onClick={() => runScenario(scenario)}><Activity size={15} /> Refresh mock feed</button><Link href="/compose" className="primary-action"><Sparkles size={16} /> Start composition</Link></div>} /><div className="stat-grid"><Stat label="Platform health" value={`${snapshot.health}%`} detail={`refreshed ${snapshot.refreshedAt}`} icon={Activity} accent="mint" /><Stat label="Applications" value={snapshot.applications.toString()} detail="4 materialized today" icon={Boxes} /><Stat label="Capabilities" value={snapshot.capabilities.toString()} detail={`${snapshot.capabilities - snapshot.futureCapabilities} native / ${snapshot.futureCapabilities} future`} icon={CircleDot} /><Stat label="External plugins" value={snapshot.plugins.length.toString().padStart(2, "0")} detail={`${snapshot.plugins.filter(plugin => plugin.status === "validated").length} validated`} icon={PlugZap} accent="amber" /></div><section className="panel activity-panel"><div className="panel-head"><div><span className="eyebrow">System activity</span><h3>Recent operations</h3></div><Link href="/audit" className="text-link">View audit <ChevronRight size={14} /></Link></div>{snapshot.activities.map(activity => <ActivityRow key={activity.id} title={activity.title} meta={activity.meta} status={activity.status} tone={activity.status === "pending" ? "amber" : "green"} />)}</section></div><ContextPanel title="Platform status"><div className="health-score"><div><strong>{snapshot.health}</strong><span>/ 100</span></div><div className="health-ring"><span>nominal</span></div></div><div className="context-section"><span className="eyebrow">Lifecycle</span><Signal label="Generated" value={snapshot.lifecycle.generated.toString().padStart(2, "0")} tone="green" /><Signal label="Validated" value={snapshot.lifecycle.validated.toString().padStart(2, "0")} tone="green" /><Signal label="Preview" value={snapshot.lifecycle.preview.toString().padStart(2, "0")} tone="amber" /><Signal label="Production" value={snapshot.lifecycle.production.toString().padStart(2, "0")} tone="muted" /></div><div className="context-note mint-note"><ShieldCheck size={16} /><p>External capabilities are pinned, validated, and provenance-tagged.</p></div></ContextPanel></div>; }

function BusinessDetail({ business, onClose }: { business: Business; onClose: () => void }) { const integrations = MOCK_INTEGRATIONS.filter(integration => business.integrationIds.includes(integration.id)); const pipelines = MOCK_PIPELINES.filter(pipeline => pipeline.businessId === business.id); return <div className="detail-drawer" role="dialog" aria-label={`${business.name} detail`}><div className="drawer-head"><div><span className="eyebrow">Business detail</span><h3>{business.name}</h3></div><button className="icon-button" onClick={onClose} aria-label="Close business detail"><X size={17} /></button></div><div className="drawer-profile"><span className="avatar business-avatar">{business.name.split(" ").map(word => word[0]).join("").slice(0, 2)}</span><div><strong>{business.industry}</strong><small>{business.environments.length} environments · {business.capabilities.length} capabilities</small></div><span className={`status-pill ${business.health === "healthy" ? "green" : "amber"}`}><span />{business.health}</span></div><div className="drawer-section"><span className="eyebrow">Connected systems</span>{integrations.map(integration => <div className="drawer-row" key={integration.id}><PlugZap size={14} /><span>{integration.connector.provider}</span><small>{integration.lifecycle} · {integration.connector.authMethod}</small></div>)}</div><div className="drawer-section"><span className="eyebrow">Pipeline topology</span>{pipelines.map(pipeline => <div className="drawer-pipeline" key={pipeline.id}><strong>{pipeline.name}</strong><div>{pipeline.stages.map((stage, index) => <span key={stage}><i />{stage}{index < pipeline.stages.length - 1 && <b />}</span>)}</div></div>)}</div><div className="context-note mint-note"><ShieldCheck size={15} /><p>Credential material is not exposed in the dashboard. Only references, status, and rotation metadata are visible.</p></div></div>; }
function ListPage({ kind }: { kind: string }) { const data: Record<string, { title: string; eyebrow: string; desc: string; icon: any; items: [string,string,string,string][] }> = { businesses: { title: "Know who is connected.", eyebrow: "Integration plane / businesses", desc: "Organisation directory across environments, capabilities, health, and governed system connections.", icon: FolderKanban, items: MOCK_BUSINESSES.map(business => [business.name, business.industry, business.health, `${business.integrationIds.length} integrations · ${business.recentActivity}`] as [string,string,string,string]) }, integrations: { title: "Every connection has a boundary.", eyebrow: "Integration plane / registry", desc: "Connection catalogue with provider, authentication, lifecycle, health, and credential-reference metadata.", icon: PlugZap, items: MOCK_INTEGRATIONS.map(integration => [integration.connector.provider, `${integration.businessId} · ${integration.connector.authMethod}`, integration.lifecycle, `${integration.connector.category} · last sync ${integration.lastSuccessfulSync}`] as [string,string,string,string]) }, pipelines: { title: "Make the business flow legible.", eyebrow: "Integration plane / pipelines", desc: "Visual pipeline definitions from trigger through transformation, validation, execution, and output.", icon: Workflow, items: MOCK_PIPELINES.map(pipeline => [pipeline.name, pipeline.businessId, pipeline.lifecycle, `${pipeline.throughput} · last run ${pipeline.lastExecution}`] as [string,string,string,string]) }, executions: { title: "See every run, retry, and provenance chain.", eyebrow: "Integration plane / executions", desc: "Operational telemetry for pipeline execution IDs, duration, records, retries, errors, and origin.", icon: Activity, items: MOCK_EXECUTIONS.map(execution => [execution.id, execution.timestamp, execution.status, `${execution.recordsProcessed} records · ${execution.duration} · ${execution.retryCount} retries`] as [string,string,string,string]) }, applications: { title: "Applications in motion.", eyebrow: "Application plane / registry", desc: "Track generated systems from first composition through validated delivery.", icon: Boxes, items: [["Enterprise Portal", "enterprise", "Ready", "identity · organisations · authorization"], ["Procurement Hub", "procurement", "Composed", "identity · storage · audit"], ["Risk Console", "enterprise + plugin", "Draft", "identity · risk-scoring"]] }, pathways: { title: "Find the right starting point.", eyebrow: "Control plane / pathways", desc: "Every ONENESS pathway is a composed system boundary with visible capability intent.", icon: Network, items: [["Enterprise", "foundation", "8 capabilities", "identity · organisations · authorization"], ["Procurement", "foundation", "5 capabilities", "rfq · supplier-lifecycle · audit"], ["Digital Twin", "foundation", "5 capabilities", "twin-state · simulation · alerts"]] }, capabilities: { title: "A marketplace of system primitives.", eyebrow: "Composition plane / capabilities", desc: "Native, plugin, and future modules are shown together without hiding their origin.", icon: CircleDot, items: [["Identity", "native", "partial", "authentication · profiles"], ["Authorization", "native", "partial", "rbac · abac · policy-engine"], ["Risk Scoring", "plugin:acme-risk@1.0.0", "partial", "risk-models · decision-support"], ["Advanced Banking", "roadmap", "future", "settlement · reconciliation"]] }, plugins: { title: "Keep the extension surface governed.", eyebrow: "Control plane / plugins", desc: "External packages are trusted application code, explicitly registered and provenance-visible.", icon: PlugZap, items: [["Acme Risk", "acme-risk", "Validated", "8 capabilities · pinned v1.0.0"], ["AI Analytics", "ai-analytics", "Validated", "5 capabilities · pinned v2.1.0"], ["Commerce Bridge", "commerce-bridge", "Pending", "3 capabilities · awaiting review"]] }, projects: { title: "Generated projects, made legible.", eyebrow: "Application plane / projects", desc: "Browse composition manifests and generated capability modules before deployment.", icon: FolderKanban, items: [["enterprise-app", "materialized", "Build passed", "14 files · 3 modules"], ["procurement-app", "validated", "Tests passed", "12 files · 3 modules"], ["risk-console", "draft", "Not validated", "16 files · 4 modules"]] }, audit: { title: "Know what changed and why.", eyebrow: "Service plane / audit", desc: "A durable operational timeline for composition, plugins, materialization, and deployment gates.", icon: ShieldCheck, items: [["User authenticated", "20:14", "verified", "session · user_123"], ["Enterprise composition created", "20:15", "recorded", "pathway · enterprise"], ["Plugin loaded", "20:16", "recorded", "acme-risk@1.0.0"], ["Application materialized", "20:17", "recorded", "enterprise-app"]] }, deployment: { title: "Move forward without skipping a gate.", eyebrow: "Delivery plane / deployment", desc: "Generated is not production. The platform keeps validation, preview, approval, and production distinct.", icon: Rocket, items: [["Enterprise App", "generated", "Ready", "typecheck · build · tests passed"], ["Procurement App", "validated", "Preview next", "security review pending"], ["Risk Console", "draft", "Blocked", "unresolved plugin dependency"]] } }; const [selectedBusiness, setSelectedBusiness] = useState<Business | null>(null); const baseView = data[kind] || data.applications; const view = kind === "pathways" ? { ...baseView, items: MOCK_SNAPSHOT.pathways.map(pathway => [pathway.name, pathway.status, `${pathway.capabilityCount} capabilities`, pathway.capabilities.join(" · ")] as [string, string, string, string]) } : kind === "plugins" ? { ...baseView, items: MOCK_SNAPSHOT.plugins.map(plugin => [plugin.name, `${plugin.id}@${plugin.version}`, plugin.status, `${plugin.capabilityCount} capabilities · ${plugin.dependencies.join(" · ")}`] as [string, string, string, string]) } : baseView; const Icon = view.icon; return <div className="page-grid"><div className="page-main"><PageHeading eyebrow={view.eyebrow} title={view.title} description={view.desc} action={kind === "applications" ? <Link href="/compose" className="primary-action"><Sparkles size={16} /> New application</Link> : undefined} /><section className="panel list-panel"><div className="list-toolbar"><div className="search compact"><Search size={15} /><input placeholder={`Search ${kind}`} /></div><span className="mono muted">{view.items.length.toString().padStart(2, "0")} records</span></div>{view.items.map(([title, meta, status, detail]) => <div className={`resource-row ${kind === "businesses" ? "is-clickable" : ""}`} key={title} onClick={() => kind === "businesses" && setSelectedBusiness(MOCK_BUSINESSES.find(business => business.name === title) || null)} role={kind === "businesses" ? "button" : undefined} tabIndex={kind === "businesses" ? 0 : undefined}><div className="resource-route"><span /></div><div className="resource-icon"><Icon size={17} /></div><div className="resource-main"><strong>{title}</strong><span className="mono">{meta}</span><small>{detail}</small></div><span className={`status-pill ${statusTone(status)}`}><span />{status}</span><ChevronRight size={16} className="row-arrow" /></div>)}</section>{selectedBusiness && <BusinessDetail business={selectedBusiness} onClose={() => setSelectedBusiness(null)} />}</div><ContextPanel title={`${kind} summary`}><Signal label="Active" value={kind === "audit" ? "24" : "09"} tone="green" /><Signal label="Pending review" value="03" tone="amber" /><Signal label="Future scope" value="07" tone="muted" /><div className="context-note"><FileCode2 size={16} /><p>Open a record to inspect its manifest, dependencies, and provenance trail.</p></div></ContextPanel></div>; }

function Placeholder({ kind }: { kind: string }) { const titles: Record<string,string> = { ai: "AI Studio is capability-controlled.", data: "Data and events, in one trace.", devices: "Devices with a visible twin state.", workflows: "Workflows that show every gate.", settings: "Settings that keep the runtime honest." }; return <div className="page-grid"><div className="page-main"><PageHeading eyebrow={`Service plane / ${kind}`} title={titles[kind] || "Settings"} description="This control-center surface is scaffolded for the next platform integration phase." /><section className="empty-state panel"><div className="empty-orbit"><Terminal size={28} /></div><span className="eyebrow">Coming next</span><h3>Native platform surface</h3><p>The UI boundary is ready. Runtime providers, event adapters, and policy-backed actions remain intentionally gated until their services are implemented.</p><button className="secondary-action" onClick={() => undefined}><LifeBuoy size={15} /> Read the boundary</button></section></div><ContextPanel title="Boundary"><Signal label="UI shell" value="ready" tone="green" /><Signal label="Runtime adapter" value="future" tone="amber" /></ContextPanel></div>; }

function Router() { return <Switch><Route path="/" component={Dashboard} /><Route path="/compose" component={Composer} /><Route path="/businesses"><ListPage kind="businesses" /></Route><Route path="/integrations"><ListPage kind="integrations" /></Route><Route path="/pipelines"><ListPage kind="pipelines" /></Route><Route path="/executions"><ListPage kind="executions" /></Route><Route path="/applications"><ListPage kind="applications" /></Route><Route path="/pathways"><ListPage kind="pathways" /></Route><Route path="/capabilities"><ListPage kind="capabilities" /></Route><Route path="/plugins"><ListPage kind="plugins" /></Route><Route path="/projects"><ListPage kind="projects" /></Route><Route path="/audit"><ListPage kind="audit" /></Route><Route path="/deployment"><ListPage kind="deployment" /></Route><Route path="/:kind"><Placeholder kind="ai" /></Route><Route component={NotFound} /></Switch>; }

export default function App() { return <ThemeProvider defaultTheme="dark"><TooltipProvider><AppShell><Router /></AppShell></TooltipProvider></ThemeProvider>; }
